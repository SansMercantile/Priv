apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: agi-core-cloudrun
spec:
  template:
    spec:
      containers:
      - image: agi-core:latest
        env:
        - name: ENVIRONMENT
          value: "production"
        ports:
        - containerPort: 8000
