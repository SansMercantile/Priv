import React from 'react';

export default function WalletPage(){
    return (
        <div className="p-6">
            <h1 className="text-2xl font-bold mb-4">Wallet Vault</h1>
            <p className="text-gray-400">Overview of wallets, balances, and vault interactions. This will integrate with the backend `wallet_vault` when available.</p>
        </div>
    );
}
